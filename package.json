{
  "name": "stackcert-product",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix web ci --workspaces=false --include=optional && npm --prefix web run build",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
